{
  "name": "tauri-plugin-authium-api",
  "version": "0.1.0",
  "author": "grcq",
  "description": "",
  "type": "module",
  "types": "./dist-js/index.d.ts",
  "main": "./dist-js/index.cjs",
  "module": "./dist-js/index.js",
  "exports": {
    "types": "./dist-js/index.d.ts",
    "import": "./dist-js/index.js",
    "require": "./dist-js/index.cjs"
  },
  "files": [
    "dist-js",
    "README.md"
  ],
  "scripts": {
    "build": "rollup -c",
    "prepublishOnly": "pnpm build",
    "pretest": "pnpm build",
    "release:patch": "pnpm build && pnpm version patch && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:minor": "pnpm build && pnpm version minor && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:major": "pnpm build && pnpm version major && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:alpha:minor": "pnpm build && pnpm version preminor --preid=alpha && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:beta:minor": "pnpm build && pnpm version preminor --preid=beta && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:rc:minor": "pnpm build && pnpm version preminor --preid=rc && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:alpha:major": "pnpm build && pnpm version premajor --preid=alpha && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:beta:major": "pnpm build && pnpm version premajor --preid=beta && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:rc:major": "pnpm build && pnpm version premajor --preid=rc && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:alpha:patch": "pnpm build && pnpm version prepatch --preid=alpha && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:beta:patch": "pnpm build && pnpm version prepatch --preid=beta && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public",
    "release:rc:patch": "pnpm build && pnpm version prepatch --preid=rc && cargo set-version $(node -p \"require('./package.json').version\") && cargo publish --dry-run && cargo publish && pnpm publish --access public"
  },
  "dependencies": {
    "@tauri-apps/api": ">=2.0.0-beta.6"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.0.0",
    "rollup": "^4.9.6",
    "typescript": "^5.3.3",
    "tslib": "^2.6.2"
  }
}